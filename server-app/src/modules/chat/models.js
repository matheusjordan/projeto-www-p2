class Chat {
    constructor(id, name, messages, createdAt) {
        this.id = id;
        this.name = name;
        this.messages = messages;
        this.createdAt = createdAt;
    }
}

module.exports = Chat;